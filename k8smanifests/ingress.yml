apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-service-ingress
  namespace: default
spec:
  ingressClassName: nginx-example
  rules:
  - host: vm.suryanshverma.local
    http:
        paths:
        - path: /testpath
          pathType: Prefix
          backend:
            service:
              name: my-app
              port:
                number: 6901


